# 复制为 .env.production 后执行 npm run build
# 前后端同域（推荐）：留空，浏览器请求当前站点下的 /api、/ws、/api/v1
# VITE_API_BASE_URL=

# 仅当静态资源与 API 不同域时填写（不要末尾斜杠，路径里已含 /api）
# VITE_API_BASE_URL=https://api.example.com

# WebSocket 可选覆盖（默认同源 ws(s)://当前 host/ws）
# VITE_WS_URL=wss://api.example.com/ws
